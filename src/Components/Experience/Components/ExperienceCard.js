import React from "react";
import "./ExperienceCard.css";

function ExperienceCard({ frontContent, backContent }) {
    return (
      <div className="experience-card">
        
      </div>
    );
  }

export default ExperienceCard;
